/**@typedef {import('./core').HsHTMLElement} HsHTMLElement*/

import { HsHTMLElement } from './core'

/**
 * @param {HsHTMLElement[] | null} children */

export const div = (children) => {
  let element = new HsHTMLElement(name = "div", children = children)
}
